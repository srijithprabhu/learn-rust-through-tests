mod hello_world;
mod string_formatting;
mod tuples_arrays_and_slices;
mod structs;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
